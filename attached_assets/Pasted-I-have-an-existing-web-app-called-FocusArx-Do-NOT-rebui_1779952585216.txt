I have an existing web app called FocusArx. Do NOT rebuild it from scratch.
Upgrade it by adding the following 5 features on top of the existing codebase.
Preserve all current functionality, styling, and data.

---

## UPGRADE 1 — DYNAMIC RESCHEDULING ENGINE
In the existing task/session planner, add:
- Real-time overrun detection: if a task timer exceeds its estimated duration, 
  trigger a modal: "You're X minutes over. Auto-reschedule remaining tasks?"
- On confirm, automatically compress/shift all remaining time blocks to fit the 
  rest of the day proportionally
- Add an animated horizontal timeline (Gantt-style) that visually updates when 
  blocks shift — smooth CSS transition on bar widths and positions
- If the total overrun makes completion impossible, highlight which tasks to 
  defer and prompt: "Move to tomorrow or drop?"

---

## UPGRADE 2 — CONTEXTUAL WHITELISTING (LOCATION & NETWORK PROFILES)
Add a new "Profiles" section to the app:
- User creates named profiles: e.g., "Office", "Home", "Cafe"
- Each profile stores: a Wi-Fi SSID name, a blocked domains list, 
  and a whitelist exceptions list
- Backend reads a config file / settings table for SSID matching; 
  user sets their SSIDs in Settings
- When profile changes (manual or auto-detected), animate a card flip: 
  "Profile Activated: Office Mode 🔒"
- Add a persistent top-bar shield indicator showing active profile 
  name + number of active blocks

---

## UPGRADE 3 — FORCED COMMITMENT MECHANISMS
Upgrade the "Start Focus Session" flow with a commitment level picker:
- 🟡 Soft Lock — 10-second delay before cancel is allowed
- 🔴 Hard Lock — user must type a custom exit phrase they set themselves
- ⚫ Beast Mode — 3-step math puzzle + 30-second wait to exit
During any locked session:
- Full-screen focus overlay activates with breathing animation circle, 
  task name in large typography, countdown ring timer
- On window blur or tab switch: play a soft chime + flash a red border 
  warning "Stay present. You've got this."

---

## UPGRADE 4 — SMART FRICTION & REFLECTION
Add a Distraction Journal system:
- When user ends a session early OR marks themselves distracted, show a 
  full-screen reflection card:
  Step 1: "What pulled you away?" (Boredom / Anxiety / Hunger / Notification / Habit / Other)
  Step 2: "Was it worth it?" (Yes / No)
  Log both answers with timestamp and session context to the database
- Add a "Distraction Patterns" page: bar chart by category + bar chart 
  by time of day (use existing charting library or add Chart.js)
- Smart Nudge: detect recurring distraction times from logs; show a 
  proactive card 15 minutes before that time slot: 
  "Your focus usually dips around now. Start a block before the slump?"

---

## UPGRADE 5 — FOCUS READINESS SCORE & BRAIN STATE
Add a daily readiness check-in widget to the dashboard:
- Every day, prompt user to log: Sleep quality (1–5), Stress (1–5), Energy (1–5)
- Calculate a Readiness Score (0–100) and recommend session length:
  - 80+: Full 90-min deep work blocks
  - 50–79: 45-min blocks, 10-min breaks
  - Below 50: 25-min Pomodoro + 15-min rest + walk reminder
- Optional manual HRV input field (from Apple Health / Garmin) that 
  adjusts recommendations
- Show a circular score gauge widget on dashboard with gradient fill, 
  animated on load, with a personalized message based on score
- Recovery Mode: if 2+ consecutive low-readiness days logged, switch to 
  gentle mode — shorter sessions, more breaks, affirmations between blocks

---

## UPGRADE 6 — AI FOCUS COACH (SIDEBAR)
Add a collapsible coach chat panel to the existing UI:
- Uses Anthropic Claude API (model: claude-sonnet-4-20250514)
- API key stored as environment variable ANTHROPIC_API_KEY
- System prompt: "You are FocusArx Coach, an expert productivity and 
  neuroscience coach. You have access to the user's current task, 
  readiness score, active profile, and recent distraction patterns. 
  Be warm, sharp, and motivating. Under 100 words unless asked for more."
- Inject into every API call: current task name, time remaining, 
  readiness score, last 3 distractions
- Coach proactively sends one tip per session start based on context
- UI: glassmorphism dark panel, smooth message bubbles, typing indicator

---

## IMPORTANT
- Do NOT change FocusArx's existing design language, color scheme, or navigation
- Match all new UI elements to the current app's visual style
- All new data goes into the existing database (add new tables/columns as needed)
- Each upgrade should be a self-contained addition — do not break existing features
- After completing all upgrades, confirm which features were added and list 
  any new environment variables or dependencies required