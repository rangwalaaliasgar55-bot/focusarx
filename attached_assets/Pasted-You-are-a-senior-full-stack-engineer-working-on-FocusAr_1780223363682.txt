You are a senior full-stack engineer working on FocusArx. Do NOT rebuild or restructure anything that already exists. Add the "Break Free" feature — a digital wellness module to help users reduce pornography consumption. Integrate it fully into the existing stack.
Stack reminder: React 19 + Vite + Tailwind CSS 4 + Framer Motion (frontend) | Express 5 + Drizzle ORM + Neon PostgreSQL (backend) | JWT auth already in place | OpenAPI spec is source of truth — run codegen after any spec changes.

STEP 1 — DB Schema (lib/db/src/schema/focusarx.ts)
Add these new tables using Drizzle ORM, following the exact same patterns as existing tables (UUID primary key via crypto.randomUUID(), CASCADE on delete, timestamps):
ts// Porn-free streak tracker (one row per user)
export const breakFreeStreaksTable = pgTable("break_free_streaks", {
  id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  userId: text("user_id").notNull().unique().references(() => usersTable.id, { onDelete: "cascade" }),
  startDate: text("start_date").notNull(), // ISO date string "YYYY-MM-DD"
  currentStreak: integer("current_streak").notNull().default(0),
  longestStreak: integer("longest_streak").notNull().default(0),
  relapseCount: integer("relapse_count").notNull().default(0),
  lastRelapseDate: text("last_relapse_date"),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});

// Daily mood check-ins
export const breakFreeMoodsTable = pgTable("break_free_moods", {
  id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  userId: text("user_id").notNull().references(() => usersTable.id, { onDelete: "cascade" }),
  mood: integer("mood").notNull(), // 1–5 scale
  date: text("date").notNull(), // "YYYY-MM-DD"
  createdAt: timestamp("created_at").defaultNow().notNull(),
});

// Anonymous public pledge wall
export const breakFreePledgesTable = pgTable("break_free_pledges", {
  id: text("id").primaryKey().$defaultFn(() => crypto.randomUUID()),
  message: text("message").notNull(), // max 100 chars
  postedAt: timestamp("posted_at").defaultNow().notNull(),
  // NO userId — fully anonymous
});
After adding the schema run: pnpm --filter @workspace/db run push

STEP 2 — OpenAPI Spec (lib/api-spec/openapi.yaml)
Add these endpoints to the spec following existing patterns:
GET    /break-free/streak          → get current user's streak data
POST   /break-free/streak/start    → set start date (body: { startDate: string })
POST   /break-free/streak/relapse  → reset streak, increment relapseCount
GET    /break-free/moods           → get last 7 days of moods for user
POST   /break-free/moods           → log today's mood (body: { mood: 1–5 })
GET    /break-free/pledges         → get latest 20 anonymous pledges
POST   /break-free/pledges         → post anonymous pledge (body: { message: string })
After updating the spec run: pnpm --filter @workspace/api-spec run codegen

STEP 3 — API Routes (artifacts/api-server/src/routes/breakFree.ts)
Create a new route file. Follow the exact same patterns as sessions.ts and readiness.ts:

Use extractUserId from auth for all routes except GET /pledges and POST /pledges (pledges are fully anonymous — no auth required)
Validate inputs with Zod
Use Drizzle ORM for all DB operations
Auto-calculate currentStreak from startDate to today on every GET
On POST /relapse: save current streak to longestStreak if it's bigger, set startDate to today, increment relapseCount
On POST /pledges: sanitize message (strip offensive words using a simple blocklist), enforce max 100 chars
Register the router in artifacts/api-server/src/routes/index.ts as /break-free


STEP 4 — Frontend Page (artifacts/focusarx/src/pages/break-free.tsx)
Create a full page at route /break-free. Add it to the router in App.tsx. Add a nav link in AppShell.tsx (sidebar/bottom nav). Use existing UI components from src/components/ui/. Use TanStack Query hooks (generated from codegen) for all API calls. Use Framer Motion for animations.
The page has 5 sections — build each as its own component:
Section 1 — Streak Counter (BreakFreeStreak.tsx)

Big centered day counter: "Day 0", "Day 7", "Day 30" with animated number
If no streak started: show a single "Start My Journey" button → calls POST /break-free/streak/start with today's date
Milestone badges at Day 3 🌱, Day 7 ⚡, Day 14 🔥, Day 30 🏆, Day 60 💎, Day 90 🌟 — show with a glow animation when milestone is hit
Milestone messages: Day 3 "Your brain is already adapting", Day 7 "One week — dopamine is rebalancing", Day 14 "Two weeks! Focus and sleep are improving", Day 30 "One month. You've rewired your reward system", Day 60 "Two months. Relationships and confidence soar here", Day 90 "90 days. You've genuinely changed your brain."
"I relapsed" button — subtle, not prominent. On click: show a warm Dialog saying "That's okay. Every restart is strength. Day 1 again — you've got this." with a confirm button → calls POST /break-free/streak/relapse

Section 2 — Urge Surfing Tool (UrgeSurfing.tsx)

Big warm button: "I'm struggling right now 🌊"
Opens a full-screen Framer Motion overlay (dark, calming background — deep blue/green gradient)
Inside the overlay:

Breathing circle — animated CSS circle that expands/contracts on the 4-7-8 pattern (inhale 4s, hold 7s, exhale 8s). Label shows "Inhale", "Hold", "Exhale" with a progress ring
5-minute countdown timer — large digital clock counting down from 5:00
Distraction suggestions — 4 cards: "Drink cold water 💧", "Do 20 pushups 💪", "Go for a walk 🚶", "Call someone you trust 📞"
Close button appears after 60 seconds (prevents instant dismissal)



Section 3 — Why It Matters (WhyItMatters.tsx)

4 horizontal scrollable science cards. No auth needed to read. Clean card design with icon + title + 2–3 sentence explanation:

🧠 "The Dopamine Loop" — excessive porn spikes dopamine unnaturally, dulling your brain's response to real-life rewards over time
📅 "Your Brain's Timeline" — Day 7: withdrawal eases. Day 14: focus returns. Day 30: motivation rebuilds. Day 90: baseline dopamine resets
❤️ "Real Relationships Win" — studies link high porn use to reduced intimacy satisfaction and emotional disconnection
🙋 "You Are Not Broken" — this is one of the most common struggles globally. Millions are fighting it silently. You are not alone



Section 4 — Mood Check-in (MoodCheckin.tsx)

Shows at top of page daily (once per day — check if today's mood already logged)
5 emoji buttons: 😔 😐 🙂 😊 😄 (values 1–5)
On tap: calls POST /break-free/moods, shows a subtle toast "Mood logged ✓"
Below: a simple 7-day bar chart (use recharts, already available) showing mood history. Color the bars by mood level (red → green gradient)

Section 5 — Pledge Wall (PledgeWall.tsx)

Show latest 20 anonymous pledges in a scrollable feed
Each pledge: soft card, message text, relative time ("2 hours ago", "Day 14 of someone's journey" — just use postedAt for relative time)
Input box (max 100 chars) + "Post Anonymously" button
No name, no account required to post
On submit: calls POST /break-free/pledges, clears input, refreshes list
Auto-block offensive words client-side before sending


STEP 5 — Design Tone

Match FocusArx's existing dark theme and Tailwind design system
Color palette for this section: soft teals and deep blues — calming, NOT clinical
Typography: warm and human — not cold/corporate
All messages: kind, science-based, zero shame
Mobile-first — most usage will be on phones


Rules

❌ Do NOT touch existing tables, routes, or components
❌ Do NOT add Google Auth or any OAuth
❌ No localStorage for this feature — all data in Neon DB
✅ Follow the contract-first pattern — update OpenAPI spec first, then run codegen
✅ Use existing usersTable foreign keys with CASCADE
✅ Pledge wall needs zero auth — anyone can read/post
✅ All other routes require valid JWT (use extractUserId)


