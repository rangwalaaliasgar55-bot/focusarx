{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.26.1",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm -r --if-present run build",
    "build:vercel": "pnpm --filter @workspace/db run push:vercel && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/focusarx run build:vercel",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "test:a11y": "playwright test tests/e2e/accessibility.spec.ts",
    "db:push": "pnpm --filter @workspace/db run push",
    "dev": "pnpm --filter @workspace/api-server run dev & sleep 3 && pnpm --filter @workspace/focusarx run dev"
  },
  "private": true,
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
