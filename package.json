{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:vercel": "pnpm run typecheck:libs && pnpm --filter @workspace/focusarx run typecheck && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/focusarx run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
