We are using Replit STRICTLY as a local development workspace, debugging environment, and code editor for the Focus Arx platform.

IMPORTANT:

* Do NOT migrate deployment away from Vercel.
* Do NOT modify production database infrastructure unnecessarily.
* Do NOT replace existing deployment architecture unless explicitly instructed.
* Production deployment pipeline MUST remain GitHub → Vercel.
* Replit is ONLY for development, auditing, debugging, testing, and code fixes.

Your role:
Act as a Senior Full-Stack Architect, UI/UX Engineer, DevOps Validator, and TypeScript debugging specialist.

Your objective:
Audit, repair, optimize, and modernize the current Focus Arx codebase so it is production-ready, scalable, visually premium, and deployable through Vercel without build failures.

────────────────────────────────────────────────────────

# CORE PRODUCT DIRECTION — FOCUS ARX

Focus Arx is an AI-powered productivity and execution platform.

The UI/UX should feel:

* premium
* futuristic
* deeply modern
* smooth
* distraction-free
* dark-first
* productivity-oriented

Primary design language:

* dark glassmorphism
* soft gradients
* minimal aesthetic
* smooth animations
* clean spacing
* modern typography
* AI-native interface patterns

Theme system requirements:

* Default theme = dark mode
* Users MUST be able to switch themes dynamically
* Include:

  * Dark Mode
  * Light Mode
  * AMOLED / Pure Black mode
  * System Theme Sync
* Persist theme preferences using local storage or user settings
* Ensure zero hydration mismatch issues

Use modern UI standards inspired by:

* Linear
* Notion
* Raycast
* Vercel
* ChatGPT
* Arc Browser

────────────────────────────────────────────────────────

# 1. APP SHELL & SIDEBAR ARCHITECTURE

Audit:

* src/components/AppShell.tsx
* src/components/Sidebar.tsx

Requirements:

* Eliminate ALL TypeScript TS7030 errors permanently.
* Every conditional block, map loop, or render branch MUST explicitly return JSX or null.
* Ensure strict TypeScript compatibility with no implicit undefined returns.

Sidebar behavior:
Desktop:

* Fixed persistent sidebar
* Smooth hover transitions
* Collapsible state support
* Optimized spacing/icons

Mobile (<768px):

* Transform into animated slide-out drawer
* Include:

  * backdrop overlay
  * close-on-backdrop-click
  * close button ("X")
  * touch-friendly interactions
  * smooth Framer Motion transitions

Branding:

* Use /public/logo.png
* Ensure proper responsive scaling
* Prevent layout shift

Performance:

* Avoid unnecessary re-renders
* Lazy-load heavy sections if needed

────────────────────────────────────────────────────────

# 2. GLOBAL UI/UX MODERNIZATION

Perform a full UI audit and modernize weak/outdated interfaces.

Requirements:

* Consistent spacing system
* Consistent typography hierarchy
* Responsive layout behavior
* Premium dark UI polish
* Accessible contrast ratios
* Smooth hover/focus states
* Skeleton loaders
* Better empty states
* Toast feedback system
* Better loading indicators
* Improved mobile responsiveness

Implement:

* ThemeProvider architecture
* Reusable design tokens
* Shared UI primitives
* Scalable component structure

Preferred stack:

* Tailwind CSS
* shadcn/ui
* Framer Motion
* Lucide Icons

────────────────────────────────────────────────────────

# 3. AI CAMERA FEATURE DEBUGGING

The AI Camera feature is currently broken.

Fully audit:

* webcam permissions
* media device initialization
* stream cleanup
* async camera loading
* browser compatibility
* state management
* error boundaries
* AI processing pipeline
* image capture flow

Requirements:

* Camera initializes reliably
* Add proper loading states
* Add fallback error UI
* Handle denied permissions gracefully
* Prevent memory leaks from media streams
* Ensure mobile browser compatibility
* Ensure React lifecycle cleanup is correct

If AI image analysis exists:

* Verify API request flow
* Validate upload payloads
* Confirm async inference handling
* Ensure response parsing does not fail silently

────────────────────────────────────────────────────────

# 4. AI STUDY ROADMAP SYSTEM

The AI Study Roadmap feature needs a complete UX and logic audit.

Requirements:

* Improve roadmap generation flow
* Improve AI prompting structure
* Better loading/progress visualization
* Save generated roadmaps properly
* Add editable roadmap sections
* Improve structured output rendering
* Add timeline/progress tracking UI

The roadmap system should feel:

* intelligent
* visually organized
* interactive
* modern
* motivating

Potential UI ideas:

* expandable milestones
* completion tracking
* progress bars
* AI-generated checkpoints
* dynamic learning phases
* estimated completion times

────────────────────────────────────────────────────────

# 5. AUTHENTICATION & ADMIN VALIDATION AUDIT

Audit:

* login flow
* signup flow
* middleware
* auth guards
* session handling
* JWT/token parsing
* backend role validation
* protected routes

Critical issue:
Accounts with role "ADMIN" are failing authorization checks.

Requirements:

* Verify role comparisons are case-safe and type-safe
* Ensure session parsing is stable
* Prevent dropped auth state
* Verify async auth requests resolve correctly
* Ensure token decoding is reliable
* Validate route protection logic

Implement:

* Better auth error logging
* Centralized auth utilities
* Safer role enums/constants
* Proper redirect handling

────────────────────────────────────────────────────────

# 6. DATABASE & BACKEND STABILITY

Audit backend architecture for:

* API consistency
* error handling
* schema validation
* async request stability
* database query safety
* rate limiting readiness

Preferred patterns:

* modular services
* reusable utilities
* clean separation of concerns
* scalable folder structure

Ensure:

* No hidden runtime crashes
* Proper try/catch coverage
* Stable async flows
* Clean API responses

────────────────────────────────────────────────────────

# 7. VERCEL DEPLOYMENT VALIDATION

Audit:

* vercel.json
* vite.config
* tsconfig
* build scripts
* environment variable usage

Requirements:

* Ensure Vercel build compatibility
* Validate proper Vite output directory
* Prevent path mismatches
* Ensure static asset handling works
* Ensure environment variables are safely referenced
* Confirm production build passes typecheck

Expected deployment flow:
Local Dev → GitHub Push → Vercel Auto Deploy

Do NOT redesign deployment architecture unnecessarily.

────────────────────────────────────────────────────────

# 8. FINAL VERIFICATION CHECKLIST

Before completion:

* Run full typecheck
* Run production build
* Validate responsive layouts
* Validate auth flows
* Validate AI camera feature
* Validate roadmap generation
* Ensure no console errors
* Ensure no hydration errors
* Ensure no TypeScript errors
* Ensure mobile compatibility

After fixes:
Provide:

1. Summary of all fixes
2. Files changed
3. Remaining warnings/issues
4. Recommended future improvements
5. Git commit recommendations

The final codebase should feel:

* production-grade
* scalable
* modern
* clean
* AI-native
* visually premium
* Vercel-ready
* startup-quality
