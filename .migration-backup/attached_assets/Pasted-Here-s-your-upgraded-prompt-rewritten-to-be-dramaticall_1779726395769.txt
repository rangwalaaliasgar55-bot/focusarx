Here's your upgraded prompt — rewritten to be dramatically more compelling, specific, and feature-rich. It's structured to get a developer (or AI) to produce something extraordinary:

🚀 The Upgraded Prompt

You are a senior full-stack engineer and UI/UX architect. Build FocusForge — a next-generation, AI-era student productivity platform. This is not a simple dashboard. It is a complete, immersive, gamified study OS. The bar for UI quality is Notion × Linear × Duolingo. Every interaction must feel intentional, fast, and satisfying.
Use Node.js + Express backend with a RESTful API architecture. Frontend: vanilla HTML/CSS/JS with a custom design system (dark-mode first, glass-morphism accents, smooth micro-animations). No UI framework dependencies — write clean, modular, component-based vanilla code.

🏗️ ARCHITECTURE & SETUP
Backend (server.js + /api routes):

Express server with cors, body-parser, uuid
In-memory data store simulating a real DB (users, sessions, badges, coins, tasks, leaderboard)
REST endpoints: GET/POST /api/sessions, GET/POST /api/tasks, GET /api/leaderboard, GET/POST /api/badges, GET/PUT /api/user/wallet
WebSocket (ws library) for real-time events: timer sync, co-working room presence, live leaderboard updates

Frontend (modular JS files):

app.js — router + state manager (single source of truth)
timer.js — stopwatch engine with Web Workers for background accuracy
gamification.js — XP calculator, coin economy, badge unlock logic
audio.js — Web Audio API controller
charts.js — Chart.js analytics renderer
api.js — fetch wrapper for all backend calls


🎨 DESIGN SYSTEM (implement these globally)

Color palette: Deep navy #0A0F1E base, electric violet #7C3AED primary, neon teal #06D6A0 accent, warm gold #FFB800 for rewards
Typography: Inter for UI, JetBrains Mono for stats/numbers
Spacing: 4px base unit, 8/12/16/24/32/48px scale
Animations: cubic-bezier(0.34, 1.56, 0.64, 1) spring for popups, ease-out for page transitions (300ms), subtle particle effects on XP gain
Components: glassmorphism cards (backdrop-filter: blur(12px), rgba(255,255,255,0.05) bg), neon glow borders on active elements, pill badges, animated progress rings


📱 VIEW 1 — COMMAND CENTER (Main Dashboard)
⏱️ Smart Multi-Timer System

Users can run up to 4 subject timers simultaneously, each in its own card
Each timer has: subject selector (dropdown with custom subject creation), session goal setter (target minutes), auto-pause detection (idle for 60s = pause prompt), and lap/split recording
Pomodoro Mode: toggleable 25/5 cycle with visual countdown ring (SVG animated circle), sound alert on cycle end
Timer state persists across page navigation via localStorage + backend sync
Display: elapsed time in HH:MM:SS, session goal progress bar, current "flow score" (consistency metric)

🪙 Virtual Economy — The Coin Forge

Earning rules: 10 coins/5min of focus, 50 bonus coins for completing a Pomodoro, 100 coins for hitting a session goal, 500 coins for daily goal, 2× multiplier during "Power Hour" (user-set time window)
Wallet display: animated coin counter with +N floating animation on earn, total balance, daily earnings graph (sparkline)
Spending: locked for now but show a "Coming Soon — Unlock Avatar Skins, Custom Themes, Streak Freezes" store preview panel

🌱 The Focus Garden (Visual Growth Mechanic)

A living SVG illustration of a plant/garden that visually evolves:

Stage 1 (0–1h today): Seedling — small sprout
Stage 2 (1–3h): Sapling — small plant with 2 leaves
Stage 3 (3–5h): Flowering — vibrant plant with bloom
Stage 4 (5h+): Tree — full tree with animated swaying leaves


If a session is abandoned mid-way (timer started → stopped before 50% of goal): plant wilts visually, coins deducted, warning toast shown
Hover the plant to see today's growth stats

🚫 Focus Shield (Distraction Blocker Panel)

Visual list of "blocked" apps/sites (editable — user can add any domain)
Strict Mode toggle: when ON, disables the ability to stop the timer early, shows a red pulsing "LOCKED" indicator
Scheduled Blocking: set recurring block windows (e.g., "Mon–Fri, 9am–1pm")
Shows "Threats Blocked Today" mock counter that increments realistically


🏆 VIEW 2 — GLOBAL LEADERBOARD

Animated ranking table with smooth re-ordering transitions when scores change (CSS transitions on order property)
Columns: Rank (with animated rank-change indicator ▲▼), Avatar (initials circle with unique gradient per user), Username, Top Subject, Weekly XP, Total XP, Current Streak 🔥
Top 3: full-width hero cards with gold/silver/bronze gradient borders, crown icons, confetti burst animation on load
"Your Rank" sticky row: always visible at bottom, shows user's position even if not in top 10
Filters: "This Week", "This Month", "All Time" — smooth tab switch with data transition
Subject filter: filter leaderboard by subject (Math, Science, Languages, etc.)
Hovering a user row shows a mini profile card popup with their badge count and study streak


🥇 VIEW 3 — ACHIEVEMENTS VAULT
Design a full achievement system with 4 tiers (Bronze, Silver, Gold, Legendary) and 5 categories:
⏰ Time Milestones: "First Hour" (1h total), "Half-Day Scholar" (4h/day), "Century Club" (100h total), "Legendary: 1000h Sage" (1000h)
🔥 Streaks: "Spark" (3-day streak), "Flame" (7-day), "Inferno" (30-day), "Legendary: Eternal Flame" (100-day)
🎯 Session Goals: "On Target" (complete first goal), "Bullseye" (10 goals), "Sharpshooter" (50 goals), "Legendary: Perfect Record" (100% goal completion in a month)
📚 Subject Mastery: "Subject Explorer" (study 5 subjects), "Polymath" (50h across 5 subjects), "Master of One" (100h in single subject)
⚡ Special: "Night Owl" (study after midnight), "Early Bird" (study before 7am), "Weekend Warrior" (5h on weekend), "Legendary: Speedrun" (4h in one sitting)
UI Requirements:

Each badge: custom SVG icon, tier color ring, shimmer animation when newly unlocked, progress bar showing % to next unlock
Locked badges: fully desaturated + blur(2px) + lock overlay with "X more hours needed" label
"Badge Showcase": user can pin 3 badges to display on their profile/leaderboard card
Recent unlocks: toast notification + full-screen celebration overlay with particle burst


📊 VIEW 4 — ANALYTICS COMMAND CENTER
📋 Smart Task Manager

Eisenhower Matrix view: 4-quadrant drag-and-drop task organizer (Urgent+Important, Important, Urgent, Neither)
List view: standard to-do with subtasks, due dates, subject tags, priority flags
Tasks auto-linked to timer subjects — completing a task awards bonus coins
Daily planner: time-block schedule view for the day, drag tasks into time slots
Recurring task support (daily/weekly)

📈 Analytics Dashboard
Implement these 6 charts using Chart.js:

Daily Focus Hours (last 14 days) — area chart with gradient fill, click a bar to see session breakdown
Subject Distribution (this week) — animated donut chart with legend, percentage labels
Productivity Heatmap (GitHub-style, last 3 months) — intensity shows hours/day, click cell for day detail
Goal Achievement Rate (last 30 days) — line chart, target line vs actual
Focus Score Trend — composite metric (consistency + duration + completion rate), sparkline
Coin Economy Flow — bar chart showing earned vs. projected (motivational)

🎯 Personal Records Panel
Show "Personal Bests": longest single session, best daily total, longest streak, most productive hour of day, favorite subject — all with date stamps

👥 VIEW 5 — THE FORGE ROOM (Co-Working + Audio)
🌐 Virtual Study Rooms

Room cards: 4 themed rooms ("Deep Work Dungeon", "Chill Library", "Exam Grind", "Creative Flow") each with:

Live occupancy display (mock real-time, animated user dots)
Room ambient audio theme
"Join Room" button that activates a presence view


Presence view: grid of avatar circles for all "active" users, each with their subject and timer visible (mock data but realistic)
Room chat: simple message log with emoji reactions (no backend needed — mock with setTimeout messages from "other users")
Show "Focus Mode: Others can see you're studying" indicator when in a room

🎵 Soundscape Studio

6 ambient tracks: Deep Focus (binaural beats), Lo-Fi Beats, White Noise, Rain & Thunder, Coffee Shop, Nature Forest
Each with: animated waveform visualization (canvas), volume slider, track info card
Mix Mode: enable 2 sounds simultaneously with independent volume controls
Focus Timer integration: sounds auto-pause when timer is paused, resume when timer starts
Custom Pomodoro sound alerts: choose from 5 alert tones (bell, chime, digital beep, voice prompt, custom upload)


🔔 GLOBAL FEATURES (implement across all views)

Toast notification system: success (green), warning (amber), error (red), info (blue), achievement (gold + confetti) — slide-in from top-right, auto-dismiss in 4s
Keyboard shortcuts: Space = pause/resume active timer, N = new task, 1–5 = switch views, ? = show shortcuts modal
Command palette: Cmd+K opens a Spotlight-style search/command interface
Onboarding flow: first-time users see a 5-step interactive walkthrough with spotlight overlays
Dark/Light mode toggle: smooth transition with CSS custom property swaps, persisted to localStorage
PWA-ready: include manifest.json and service worker for offline timer functionality
Responsive: flawless on 320px mobile through 1440px desktop — sidebar collapses to bottom tab bar on mobile


📁 FILE STRUCTURE
focusforge/
├── server.js              # Express + WebSocket server
├── package.json
├── public/
│   ├── index.html         # Shell + navigation
│   ├── manifest.json
│   ├── sw.js              # Service worker
│   ├── css/
│   │   ├── design-system.css  # Variables, base, typography
│   │   ├── components.css     # Reusable components
│   │   └── views.css          # View-specific styles
│   └── js/
│       ├── app.js         # Router + state
│       ├── api.js         # Backend client
│       ├── timer.js       # Timer engine
│       ├── gamification.js
│       ├── audio.js
│       ├── charts.js
│       └── views/
│           ├── dashboard.js
│           ├── leaderboard.js
│           ├── achievements.js
│           ├── analytics.js
│           └── coworking.js
Write every file completely. No // TODO placeholders. No skeleton code. Every feature must be functional. Add JSDoc comments on every function. CSS must be fully responsive with no horizontal scroll at any screen width.